version: 2.1

orbs:
  maven: circleci/maven@0.0.8
  
jobs:
  build:
    docker:
      - image: java:8-alpine
    steps:
      - checkout
      - run:
          name: The First Step
          command: |
            echo 'Hello World!'
            echo 'This is the delivery pipeline'
      - run:
          name: Code Has Arrived
          command: |
            ls -al
            echo '^^^That should look familiar^^^'
      - run: 
          name: Maven Build
          command: |
            mvn clean install 


workflows:
  maven_test:
    jobs:
      - build # checkout, build, test, and upload test results
